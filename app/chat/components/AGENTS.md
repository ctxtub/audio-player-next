# AGENTS

## 目录职责
- 汇总聊天功能所需的复用组件。

## 子目录结构
- `ChatLog/`：会话记录滚动视图。
- `Composer/`：聊天输入框与发送按钮组合组件。

## 关键协作与依赖
- 样式需使用本目录下的 `*.module.scss` 并依赖全局主题变量。
- 存放聊天页面私有的功能组件，包含输入、消息及状态反馈模块。
- 组件需遵循无副作用、可组合的原则，便于在不同聊天布局中复用。
- UI 底层依赖 `antd-mobile` 组件与 `@/styles/variables` 主题变量。
