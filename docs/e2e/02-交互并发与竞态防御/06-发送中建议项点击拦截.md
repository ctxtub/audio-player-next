# E2E-02-06 · 发送中点击建议项被拦截

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-02-06` |
| 所属功能套件 | `02-交互并发与竞态防御` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 3: 核心交互并发与竞态防御（全局队列第 12 项） |
| 待验证假设 | — |

## 用例规格

- 前置条件/夹具：`{{GUEST_FRESH}}`；流式进行中（mock 放慢）。
- 步骤：1) 提交生成；2) 流式中点击任一建议项。
- UI 断言：被拦截（toast「正在生成回答」或等价提示）；输入框内容不被覆盖为建议文本。
- 音频/浏览器断言：无第二路 `agent.interact`。
- 网络/数据断言：无新增 `chat.saveConversation` 异常。
- 清理：清空会话。
- 证据要求：`ui-suggestion-guard.png`、`network.json`。
- 溯源：`app/(main)/chat/components/ChatLayout/index.tsx:138-145`（`isSending` 守卫）；`app/(main)/chat/components/Composer/Composer.tsx:148-157`。
