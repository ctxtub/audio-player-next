# E2E-01-05 · 配置初始化门控与错误屏重试

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-01-05` |
| 所属功能套件 | `01-基础冒烟与页面基线` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 2: 冒烟基线与页面基线 |
| 待验证假设 | — |

## 用例规格

- 前置条件/夹具：`{{GUEST_FRESH}}`；健康基线对照（R4 改写：`MOCK_FAIL_CONFIG=1` 为保留位，
  上游 Mock 与应用代码均无读取实现——见 [execution-isolation.md](../execution-isolation.md) §3，
  且 `config.get` 只读进程 env、不发上游请求，运行时改之需重启应用，故失败注入面运行时不可达）。
- 步骤：1) 全新访客冷启动；2) 观察门控放行与正常页；3) 代码级确认错误屏挂载域
 （`AccountSyncProvider` 包裹 `(main)`，`initError` 路径静态存在）。
- UI 断言：冷启动后进入正常页面（问候头 + 建议项 + 输入框），`isLoaded` 放行，无卡死；
  「配置加载失败」错误屏及重试入口仅作静态可达性说明（运行时不可达，不判分）。
- 音频/浏览器断言：基线会话未做生成动作，不发起 `tts`/`agent` 调用。
- 网络/数据断言：`config.get` 成功且 `isLoaded` 放行；DB `GuestConfig` 建行。
- 保留断言（UNVERIFIED，待应用层故障开关配合源码改造后启用）：注入一次 `config.get`
  失败 → 错误屏出现 → 关闭注入后点击「重试」进入正常页。
- 清理：清 cookie。
- 证据要求：`ui-healthy-baseline.png`、`network.json`。
- 溯源：`stores/configStore.ts:284-303`；`components/AccountSyncProvider/index.tsx:20-21,49-76`；`stores/accountSync.ts:98-121`。
- 实证：`20260907-145836/E2E-01-05/report.md`（UNVERIFIED：注入开关无实现）。
