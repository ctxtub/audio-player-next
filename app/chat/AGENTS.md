# AGENTS

## 目录职责
- 承载聊天体验相关的页面与组件，实现对话输入与消息呈现。
- 对接故事生成与音频播放能力，为未来聊天交互提供扩展点。

## 子目录结构
- `components/`：聊天专用的拆分组件（输入框、消息列表等）。

## 关键协作与依赖
- 与 `@/stores/**`、`@/app/services/**` 共享状态与调用业务流程。
- 复用 `@/components/**` 与 `antd-mobile` 提供的基础 UI 能力。
