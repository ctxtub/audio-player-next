# E2E-02-07 · 自动续写期间输入框被清空

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-02-07` |
| 所属功能套件 | `02-交互并发与竞态防御` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P2` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 3: 核心交互并发与竞态防御（全局队列第 20 项） |
| 待验证假设 | ⚠️ H-03 |

## 用例规格

- 前置条件/夹具：同 E2E-02-03（预载续写将触发）。
- 步骤：1) 预载触发前在输入框输入 `{{E2E_PROMPT_DRAFT}}`；2) 等待续写 dispatch 发生；3) 读输入框值。
- UI 断言：草稿**不得**被清空（若清空记为缺陷证据）。
- 音频/浏览器断言：不适用。
- 网络/数据断言：不适用。
- 清理：清空会话。
- 证据要求：`ui-draft-wiped.png`。
- 溯源：`stores/chatStore.ts:219-232`（`user.submit` 置 `inputValue:''`，与人工草稿共用同一 store 字段）。**待验证假设 H-03（输入面）**。
