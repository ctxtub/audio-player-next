# E2E-02-08 · 生成中切标签页→返回徽标一致

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-02-08` |
| 所属功能套件 | `02-交互并发与竞态防御` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 3: 核心交互并发与竞态防御（全局队列第 15 项） |
| 待验证假设 | — |

## 用例规格

- 前置条件/夹具：`{{GUEST_FRESH}}`；mock 放慢流。
- 步骤：1) 提交生成；2) 切到 `/setting`；3) 等待完成；4) 返回 `/chat`。
- UI 断言：TabBar「聊天」未读徽标出现（返回前）并在进入聊天后清除；消息无重复。
- 音频/浏览器断言：生成完成触发的自动播放继续（跨页保持）。
- 网络/数据断言：完成回调仅一次。
- 清理：清空会话。
- 证据要求：`ui-badge.png`、`audio.json`。
- 溯源：`components/MainTabBar/index.tsx:117-144`；`stores/chatStore.ts`（`hasUnviewedResponse`）。
