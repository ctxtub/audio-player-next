# AGENTS

## 目录职责
- 组合输入框与状态展示区域，负责发起故事生成请求并展示预加载状态。
- 作为首页的中枢模块连接输入、播放与预加载反馈。

## 子目录结构
- `index.tsx`：组件实现。
- `index.module.scss`：组件样式。

## 关键协作与依赖
- 依赖 `@/stores/**` 管理输入文本与预加载状态。
- 调用 `@/app/services/storyFlow` 中的方法触发业务流程。
