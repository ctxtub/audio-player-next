# AGENTS

## 目录职责
- 存放聊天页面专用的拆分组件，负责布局与容器结构组织。
- 依据功能拆分顶部栏、消息区、输入区，支持后续能力扩展。

## 子目录结构
- `ChatLayout/`：聊天页面的三段式布局容器，统筹顶部栏、消息区与输入区。

## 关键协作与依赖
- 与 `app/chat/index.tsx` 协作接受会话元数据，渲染对应 UI 占位。
- 为后续与 `@/stores/**`、Server Actions 集成预留结构化插槽。
