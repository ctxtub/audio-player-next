# E2E-01-05 · 配置初始化门控与错误屏重试

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-01-05` |
| 所属功能套件 | `01-基础冒烟与页面基线` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 2: 冒烟基线与页面基线 |
| 待验证假设 | — |

## 用例规格

- 前置条件/夹具：`{{GUEST_FRESH}}`；mock 上游注入一次 `config.get` 失败（`MOCK_FAIL_CONFIG=1`）。
- 步骤：1) 冷启动；2) 观察门控/错误屏；3) 关闭故障注入并点击「重试」。
- UI 断言：失败时出现「配置加载失败」错误屏及重试入口；重试后进入正常页面。
- 音频/浏览器断言：错误屏期间不发起 `tts`/`agent` 调用。
- 网络/数据断言：重试后 `config.get` 成功且 `isLoaded` 放行；DB `GuestConfig` 建行。
- 清理：清 cookie；移除故障注入。
- 证据要求：`ui-config-error.png`、`network.json`。
- 溯源：`stores/configStore.ts:284-303`；`components/AccountSyncProvider/index.tsx:20-21,49-76`；`stores/accountSync.ts:98-121`。
