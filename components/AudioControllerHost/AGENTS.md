# AGENTS

## 目录职责
- 挂载隐藏的音频元素并向播放 Store 注册音频控制器。
- 监听音频事件，驱动故事流程预加载与段落切换。

## 子目录结构
- `index.tsx`：音频控制宿主组件，实现音频播放与事件回调。

## 关键协作与依赖
- 调用 `@/stores/playbackStore` 注册控制器并同步进度。
- 借助 `@/app/services/storyFlow` 管理预加载、段落推进与播放状态。
