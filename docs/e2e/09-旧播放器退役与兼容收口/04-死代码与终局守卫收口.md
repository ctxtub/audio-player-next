# 死代码与终局守卫收口（M9-04，M9 收官）

功能域：09-旧播放器退役与兼容收口

## 用户目标

M9 收口：收紧 allowlist（宽目录收窄为单文件）、清“可证明由旧 Player / legacy playback compatibility 唯一拥有”的 dead 残留（`NOW_PLAYING_COMPAT_ROUTE` / `shouldSuppressNowPlayingEntry` / `createNowPlayingEntryController` 三符号 + barrel re-export + 悬挂测试断言/过期头注）、新增 M9 closure 守卫（10 条静态不变量）并做 targeted browser closure（4 条：cold bookmark / restored Anchor / active playback / canonical Work）+ Draft 回归。M9-01/02/03 已 CLOSED / FROZEN，本项不新增产品行为，不做历史大扫除。

## 前置条件

- M9-01 CLOSED / FROZEN：`/player` 只剩最薄 redirect `page.tsx`（`redirect('/library')`）。
- M9-02 CLOSED / FROZEN：旧 Player 组件已物理删除（`index.tsx` / `index.module.scss` / `components/PlaybackStatusBoard|GenerationPreview|AudioPlayer` + `components/` 目录）。
- M9-03 CLOSED / FROZEN：旧 compat adapter 三件套（`components/FloatingPlayer` / `stores/playbackProgressStore.ts` / `lib/client/playbackProgress.ts`）与 3 个 legacy procedures（`getProgress/saveProgress/clearProgress`）已删。
- 审计前置：三符号在产品代码侧 active consumer 为 0（仅 barrel re-export + 测试）；`isOneShot` / `sourceType/sourceId` / 旧 floating flags / 旧 Player config 仍被冻结模块承担兼容职责，命中即留；`lib/navigation/mainNavigation.ts` 的 `/player → library` alias 与 `middleware.ts` 的 compat 守卫为冻结兼容职责，保留并记录理由。
- 硬边界：不碰 M7 P3B Story-level Timeline、不改 M5 DB schema/identity migration、不做 M8 audio lifecycle redesign、不碰 M10 historical browser debt、不 main merge、不新增 UI 行为、不动 M5/M6/M7/M8 冻结契约。

## 操作步骤

1. 审计（逐项给 scan 命令 + 结果证据，命中才动）：
   - `rg -n "'/player'|\"/player\"" -g '*.ts' -g '*.tsx'` + 注释剥离后二次确认：执行面仅 `components/NowPlaying/useNowPlayingEntry.ts`（待删三符号）与 `lib/navigation/mainNavigation.ts`（冻结 alias，保留）；`middleware.ts`（根文件，compat 守卫，保留）。
   - `rg -n "NOW_PLAYING_COMPAT_ROUTE|shouldSuppressNowPlayingEntry|createNowPlayingEntryController" -g '*.ts' -g '*.tsx' app components lib stores`：仅 barrel + 定义 + 测试，产品 active consumer 为 0 → 可删。
   - `FloatingPlayer/useFloatingPlayer`、`playbackProgressStore`、`player/components`、`const PlaybackStatusBoard|GenerationPreview|AudioPlayer`、`P3B`（注释剥离后）全仓扫描均为 0 → 无新增删除，仅锁存。
   - `isOneShot/sourceType/sourceId` 仍在 `stores/playbackStore.ts` / `playbackSessionStore.ts` / `storyFlow.ts` / `lib/playback/legacy.ts` 承担冻结兼容 → 留 + 记录理由，不机械删除。
2. 删除三符号：`components/NowPlaying/useNowPlayingEntry.ts` 删 `NOW_PLAYING_COMPAT_ROUTE` / `shouldSuppressNowPlayingEntry` / `createNowPlayingEntryController`，保留 `useNowPlayingEntry` / `createExpandedNowPlayingEntryController` / `NowPlayingEntryController`（M7 正式入口，Mini 消费）；清 `components/NowPlaying/index.ts` barrel re-export；同文件头注改为 M9-04 现状（零 `/player` 字面量，注释剥离后为 0）。
3. 同步测试：
   - `tests/unit/now-playing/production-cutover.unit.test.ts`：P3 改为“已移除”断言（静态 + barrel + 运行时 `undefined`）；allowlist 由 `app/(main)/player/**` + `useNowPlayingEntry.ts` 收窄为单文件 `app/(main)/player/page.tsx`，同步断言目录仅含 `page.tsx` + `useNowPlayingEntry` 剥离后零字面量。
   - `tests/unit/now-playing/mini-now-playing-semantic.unit.test.ts`：移除三符号 import，M6-02-08 改写为对 live facade（`createExpandedNowPlayingEntryController`）的等价断言（同义委托 UI open、透传 target、零路由绑定）。
   - `tests/unit/now-playing/expanded-ui-state.unit.test.ts`：M7-01-03 同步清理 sanity 行与“仍保留”断言，改为“已删除”断言。
   - `tests/unit/navigation/player-compat-redirect.unit.test.ts`：allowlist 同步收窄为单文件，检测正则保留（compat 验证，不引用死符号）。
4. 新增 M9 closure oracle（L1，`tests/unit/navigation/m9-player-retirement-closure.unit.test.ts`，10 条，见验收标准；登记 runner `m9-player-retirement-closure` + catalog `exec-m9-player-retirement-closure`）。
5. Targeted browser closure（`tests/system/browser/scenarios/m9-player-retirement-closure.spec.ts`，chromium+webkit，`retries=0`，登记 `exec-l3-m9-player-retirement-closure`）：
   - A. cold bookmark：`/player` → `/library` → 无旧 Player DOM（`播放进度`/`播放速度`/`从头重播` 零计数）→ `library-page` 可用。
   - B. restored Work Anchor：直开 `/player` → `/library` → Session hydrate → Mini 出现 → open Expanded → `sessionId`/progress 对齐（`source.workId` + `nextParagraphIndex` 一致，`status=ready`，transport 空闲）。
   - C. active playback：正在播（pause 驻留）→ harness 侧真实 App Router client transition `/player?from=active#keep` → redirect `/library` → `sessionId` 不变 → transport 不被 route 主动清理（`audioUrl` 相同 + 同 document/audio marker 存活）→ Expanded 仍可操作（open/close，URL 不变）。
   - D. canonical Work：`/library` 播放 canonical segment → route nav/reload → canonical asset/session identity 无回归（`sessionId` + `workId` + `next` 对齐，无新增 `beginSession`/`tts.synthesize`）。
   - Draft 回归（integration/unit 层，既有链覆盖）：Draft ephemeral 不因 Player 删除切 canonical（`expanded-draft-transcript` / `creation-actions` / `artifact-history-rehydration` 链）。
6. 全门：`unit / integration / tooling / catalog / tsc / lint / prisma validate / build` + targeted chromium+webkit + full browser（NEW REGRESSION=0；既有 historical failures 单列 KNOWN BASELINE，不在 M9 修）。

## 验收标准

- 三符号删除 + barrel 清理：产品代码 `NOW_PLAYING_COMPAT_ROUTE|shouldSuppress|createNowPlayingEntryController` 计数 0；`useNowPlayingEntry.ts` 剥离后零 `/player`；barrel 仅保留 live facade；运行时亦为 `undefined`。
- allowlist 收紧：active production 中 `push/nav/href/router.push('/player')` 为 0（单文件 allowlist 口径）；`app/(main)/player/` 仅含 `page.tsx`；`useNowPlayingEntry.ts` 已移出 allowlist；tests/docs 仅 compat 上下文。
- M9 closure oracle 10 条全绿：
  1) `page.tsx` 为 `redirect('/library')` 且不 import legacy UI；
  2) player 目录仅 compatibility route；
  3) 三件套 + HistoryPanel legacy ownership 为 0（Chat 正式实现保留）；
  4) `FloatingPlayer`/`useFloatingPlayer` 为 0；
  5) `playbackProgressStore`/`lib/client/playbackProgress` 为 0；
  6) 产品 navigation 到 `/player` 为 0；
  7) `AudioControllerHost` 为唯一 audio owner（全局 `<audio>` 唯一）；
  8) 正式入口为 `MiniNowPlaying + openExpanded()`，Expanded 非 route（Modal，无独立路由目录）；
  9) M5 冻结签名不可变（`getAnchor/beginSession/saveCheckpoint/completeSession/clearAnchor/promoteDraftToWork/getWorkProgressBatch` + client `getWorkPlaybackProgressBatch` + 五 schema tokens + 四表 `User/GuestPlaybackAnchor` + `Story/GuestPlaybackProgress` 原样）；
  10) 执行面 `P3B` 为 0。
- Targeted browser A–D 全绿（chromium+webkit）；Draft 回归由既有 integration/unit 链覆盖。
- 全门：unit / integration / tooling / catalog / tsc / lint / prisma validate / build 全绿；full browser NEW REGRESSION=0。

## 边界与异常

- 不是历史大扫除：`isOneShot` / `sourceType/sourceId` / 旧 floating flags / 旧 Player config 若仍被冻结模块承担兼容职责，命中即留 + 记录理由，不机械删除。
- `lib/navigation/mainNavigation.ts` 的 `/player → library` alias（`resolveMainTabKey('/player')='library'`，单测锁定）与 `middleware.ts` 的 `/player` 受保护路径为冻结兼容职责，保留。
- 历史文档（`docs/specs/**`、M9-01/02/03 `01-/02-/03-*.md`、DESIGN_SPEC 遗留描述）中的旧符号提及为历史快照，不计入 active references，不在本项改写。
- Full browser 既有 historical failures（`main-navigation-route-journey` / `smoke` 仍断言旧 `/player` 落地页）单列 KNOWN BASELINE，不在 M9 修，不计入 NEW REGRESSION。
- Dead exports 仅限本轮审计面内的文件，不漫扫全仓；过期注释仅动可证明过期且贴近触碰面的（`useNowPlayingEntry.ts` 头注必须改）。

## 实现参考

- `components/NowPlaying/useNowPlayingEntry.ts`（M7 正式入口，M9-04 已删三符号）
- `components/NowPlaying/index.ts`（barrel，已清死 re-export）
- `app/(main)/player/page.tsx`（冻结 redirect，单文件 allowlist）
- `lib/navigation/mainNavigation.ts`（冻结 alias，保留 + 理由）
- `tests/unit/navigation/m9-player-retirement-closure.unit.test.ts`（本项 L1 oracle，case `m9-player-retirement-closure`）
- `tests/system/browser/scenarios/m9-player-retirement-closure.spec.ts`（本项 targeted L3，4 条）
- `tests/test-catalog.yaml`（`exec-m9-player-retirement-closure` + `exec-l3-m9-player-retirement-closure`）
- `scripts/run-tests.mjs`（`m9-player-retirement-closure` suite 登记）
