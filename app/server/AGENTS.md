# AGENTS

## 目录职责
- 存放 `app/` 内通用的 Server Actions 与服务器端辅助函数。
- 封装请求校验、错误转换等逻辑供页面与 API Route 复用。

## 子目录结构
- `index.ts`：导出服务端处理函数集合。

## 关键协作与依赖
- 依赖 `@/lib/server/**` 调用上游服务。
- 与 `@/types/**`、`@/stores/**` 协同，确保服务端与客户端状态一致。
