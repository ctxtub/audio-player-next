# E2E-03-09 · 客户端导航/前进后退→播放不中断、镜像一致

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-03-09` |
| 所属功能套件 | `03-播放状态与内核一致性` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 4: 播放状态机深度一致性（全局队列第 28 项） |
| 待验证假设 | — |

## 用例规格

- 前置条件/夹具：播放中（延续 E2E-01-03）。
- 步骤：1) chat→player→setting→chat 两次循环；2) 期间使用浏览器后退/前进各 2 次；3) 每步采样。
- UI 断言：三镜像一致（浮窗/播放页/卡片）；后退前进不产生重复挂载或状态抖动。
- 音频/浏览器断言：全程 `audio.paused=false`、`currentTime` 连续递增（同一元素，无重建）。
- 网络/数据断言：无重复 `tts.synthesize`；无重复 `config.get` 风暴。
- 清理：清空会话。
- 证据要求：`audio.json`、`ui-nav-loop.png`。
- 溯源：`app/(main)/layout.tsx:17-27`（宿主常驻）；`stores/playbackStore.ts:399-404`（`syncPlaybackState`）。
