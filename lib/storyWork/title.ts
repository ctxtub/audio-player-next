/**
 * 故事标题推导与规整（纯领域函数）
 */

export {
  normalizeStoryTitle,
  extractHeadingTitle,
  buildPromptFallbackTitle,
  resolveStoryTitle,
} from './metadata';
export type { ResolveStoryTitleParams } from './metadata';
