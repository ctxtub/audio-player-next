# E2E-03-07 · 多段落推进+单一前瞻预取+进度落库

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-03-07` |
| 所属功能套件 | `03-播放状态与内核一致性` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 4: 播放状态机深度一致性（全局队列第 27 项） |
| 待验证假设 | — |

## 用例规格

- 前置条件/夹具：`{{GUEST_FRESH}}`；`{{E2E_STORY_4P}}`；mock 音频 2s/段。
- 步骤：1) 从第 1 段播放至第 3 段中途暂停；2) 全程记录 tRPC 调用时序。
- UI 断言：段落徽标 1→2→3；PlaybackStatusBoard 预载状态出现「已预载下一段」。
- 音频/浏览器断言：段间静默 <2s；无重复合成同一段。
- 网络/数据断言：`agent.interact` 预载调用数 = 段数-1（lookahead 严格 1）；`saveProgress` 行 `nextParagraphIndex` 递增且 `remainingAllowedMs` 数值随播放递减。
- 清理：清进度行。
- 证据要求：`network.json`、`db.txt`、`audio.json`。
- 溯源：`components/AudioControllerHost/index.tsx:267-310`（自适应阈值+lookahead=1）；`stores/playbackProgressStore.ts:371-419`（预取与中止）；`:421-448`（段落推进落库）。
