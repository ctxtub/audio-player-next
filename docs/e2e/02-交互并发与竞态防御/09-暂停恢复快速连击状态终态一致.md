# E2E-02-09 · 暂停/恢复快速连击→状态终值一致

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-02-09` |
| 所属功能套件 | `02-交互并发与竞态防御` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 3: 核心交互并发与竞态防御（全局队列第 18 项） |
| 待验证假设 | ⚠️ H-01 |

## 用例规格

- 前置条件/夹具：播放中（延续 E2E-01-03）。
- 步骤：1) 快速交替点击浮窗播放/暂停 10 次（≤80ms 间隔）；2) 停在「恢复」；3) 静置 2s。
- UI 断言：终态按钮=播放中图标；无抖动残留。
- 音频/浏览器断言：终态 `audio.paused=false` 且 `currentTime` 递增；若 UI 显示播放而实际无声（或反之），记为声画分歧证据（联动 E2E-03-01）。
- 网络/数据断言：`isPlayInterruptedError` 被静默吞掉的次数与 console 一致。
- 清理：清空会话。
- 证据要求：`audio.json`（逐次采样）、`console.json`。
- 溯源：`components/FloatingPlayer/index.tsx:131-145`；`stores/playbackStore.ts:263-265,363-370`；`components/AudioControllerHost/index.tsx:29-32,159-166`。**待验证假设 H-01（连击面）**。
