# AGENTS

## 组件职责
- 展示播放倒计时，并以统一样式呈现文本/语音生成状态。
- 直接读取全局 Store 获取业务状态，定位为业务层组件。

## 对内结构
- `index.tsx`：状态面板组件实现。
- `index.module.scss`：组件私有样式。

## 上下游依赖
- 依赖 `@/stores/**` 获取播放、预加载与故事状态。
- 对外提供 `PlaybackStatusBoard` 组件供首页、播放页等复用。
