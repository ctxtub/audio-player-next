# E2E-03-12 · 设置页试音与主播放并发双声源

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-03-12` |
| 所属功能套件 | `03-播放状态与内核一致性` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P2` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 4: 播放状态机深度一致性（全局队列第 31 项） |
| 待验证假设 | ⚠️ H-13 |

## 用例规格

- 前置条件/夹具：播放中；位于 `/setting`。
- 步骤：1) 点击试音；2) 采样双元素状态；3) 离开 `/setting`。
- UI 断言：主播放 UI 不被试音干扰。
- 音频/浏览器断言：主 `<audio>` 与试音元素同时非 paused（双声源并存——当前实现行为，登记为已知行为而非必然缺陷）；离开页面后试音元素停止。
- 网络/数据断言：试音使用独立 `tts.synthesize` 调用。
- 清理：清空会话。
- 证据要求：`audio.json`（双元素）、`console.json`。
- 溯源：`app/(main)/setting/components/VoiceServiceSection.tsx:30-44,80-84`（独立 Audio 元素，卸载清理）。**待验证假设 H-13**：并发叠加是否构成可感知缺陷，由运行时+评审判定。
