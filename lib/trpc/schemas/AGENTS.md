# AGENTS

## 目录职责
- 存放 Zod Schema 定义，用于运行时数据校验。
- 确保 API 入参、表单数据与后端期望结构的一致性。

## 子目录结构
- `auth.ts`：用户认证相关数据结构校验（登录、注册等）。
- `chat.ts`：聊天相关数据校验（发送消息、历史记录查询）。
- `story.ts`：故事生成相关参数校验。
- `tts.ts`：语音合成相关参数校验。

## 关键协作与依赖
- 被 `@/lib/trpc/routers` 引用以定义 API 输入输出类型。
- 被前端组件引用以校验表单输入。
