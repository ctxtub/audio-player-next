# E2E-03-10 · 历史恢复卡回放（audioUrl 已置空）

| 属性 | 设定值 |
| --- | --- |
| 规范 ID | `E2E-03-10` |
| 所属功能套件 | `03-播放状态与内核一致性` |
| 规范层级 | 单用例规范（Canonical Test Spec） |
| 优先级 | `P1` |
| 自动化类型 | `AUT-BROWSER` |
| 执行调度 | 阶段 4: 播放状态机深度一致性（全局队列第 29 项） |
| 待验证假设 | ⚠️ H-11 |

## 用例规格

- 前置条件/夹具：`{{GUEST_FRESH}}`；已有落库会话（含故事卡，audioUrl 已被快照清空）；硬刷新恢复。
- 步骤：1) 刷新后点击该卡「播放故事」；2) 等待结束。
- UI 断言：重新合成后正常播放；卡片按钮态与实际一致；播放结束回到可再播。
- 音频/浏览器断言：结束**不得**触发自动续写下一段（恢复卡无后续段落链）。
- 网络/数据断言：`tts.synthesize` 以卡片文本重合成；结束无 `agent.interact` 续写调用。
- 清理：清空会话、清进度。
- 证据要求：`ui-restored-card.png`、`network.json`。
- 溯源：`stores/chatStore.ts:166-183`（快照 audioUrl 置空）；`app/services/storyFlow.ts:163-183`（`playStoryText` 重合成）。**待验证假设 H-11**：恢复卡结束后的续写/复位路径。
